{
  "name": "@mailtrack/outlook-addin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-worker-page.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mailtrack/shared": "*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/office-js": "^1.0.400",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
