# MailTrack Classifier Accuracy Report

> Auto-generated by `apps/backend/src/accuracy/harness.ts` (PLAN.md ADR-23). Do not edit by hand — run `npm test` in `apps/backend` to regenerate.

Generated: 2026-07-12T13:46:13.114Z

## Headline

**Classifier accuracy against the labeled corpus: 100.0%** (11/11 scenarios).

This is a measured figure over a hand-labeled set of real-world scenarios (`scenarios.ts`), balanced across cases that MUST verify (real humans) and cases that MUST NOT (Apple MPP, scanners, prefetch, bots). It measures the classifier LOGIC. It does **not** by itself prove those scenarios match every live email client — that requires the real-world client matrix in `docs/accuracy-matrix.md`, which is human-run.

## Per-verdict precision & recall

| Verdict | Precision | Recall | Scenarios |
|---|---|---|---|
| `verified_open` | 100.0% | 100.0% | 2 |
| `verified_click` | 100.0% | 100.0% | 2 |
| `machine_suspect` | 100.0% | 100.0% | 5 |
| `not_verifiable` | 100.0% | 100.0% | 2 |

## Confusion matrix (rows = expected, columns = actual)

| expected ↓ / actual → | `verified_open` | `verified_click` | `machine_suspect` | `not_verifiable` |
|---|---|---|---|---|
| `verified_open` | 2 | 0 | 0 | 0 |
| `verified_click` | 0 | 2 | 0 | 0 |
| `machine_suspect` | 0 | 0 | 5 | 0 |
| `not_verifiable` | 0 | 0 | 0 | 2 |

## Misclassifications

None — every labeled scenario was classified correctly.

## Signal reliability (what each dashboard verdict actually means)

| Signal | Reliability | Why |
|---|---|---|
| Replied | **Definitive** | A reply must be authored by a human; no sync/proxy/prefetch can produce one (ADR-21). |
| Clicked | **Very high** | A link navigation, with known link-scanners filtered out. Residual risk: an unlisted corporate scanner. |
| Read (open+click, or reply) | **Very high** | Two independent signals, plus a proven minimum engaged-time bracket (ADR-18). |
| Likely read | **High** | At least one verified open — already passed the timing/UA/ASN/burst checks that filter out prefetches and scanners (ADR-29). A genuine human-triggered render, not proof every word was read. |
| Not verifiable | **N/A (honest abstention)** | Apple MPP / scanner / prefetch detected — we decline to claim a read rather than guess. |
